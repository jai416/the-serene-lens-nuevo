[tool.poetry]
name = "the-serene-lens-bot"
version = "1.0.0"
description = "Telegram bot for The Serene Lens operations monitoring"
authors = ["jai416 <hereirajaison@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.11"
python-telegram-bot = ">=20.0"
asyncpg = ">=0.28"
python-dotenv = ">=1.0"
aiohttp = ">=3.8"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
